# CRM 运营平台 · 二期「营销 / 用户 / 权限」PRD

> **本文档范围**
> 本文档覆盖在线原型中所有带**「二期」标识（橙色 Tag）**的功能模块，是一期《基础查询 PRD》的延续。
>
> **重要分期调整（本次更新）**
> - 原属二期的**销售中心**已重新划归**三期**（原型中改为紫色「三期」标识，并新增「外呼拨号 + 通话记录」能力）。本文档不再展开销售中心，仅在「与三期的衔接」中说明。
> - 二期当前功能清单（原型「二期」标识对应）：
>   1. **用户中心·二期**（升级）：业务线 · 渠道 · 渠道 code 精准归因、业务线全局筛选、用户分流；**新增用户详情页与课程信息 / 课时报告 / 回放**；用户状态由 3 态升级为 **5 态**。
>   2. **营销中心**（一级菜单分组，含 4 个子模块）：
>      - **渠道管理**：业务线 → 渠道类型 → 一/二/三级渠道树，任意层级生成 7 位渠道 code（自动去重），末级可填渠道参数。
>      - **商品包**：商品包自助配置、上下架。
>      - **优惠券**：满减券配置，一券多码（按 KOL 分发、分别统计使用量结算）。
>      - **落地页**：关联渠道 / 商品包 / 优惠码，一键生成投放链接；**新增落地页名称、划线价**。
>   3. **系统配置 · 角色权限**（新增）：RBAC（无权限 / 只读 / 可操作）、业务线数据隔离、成员账号、操作日志。
>
> **本次更新要点（相对上一版二期 PRD）**
> - 销售中心 → 三期（见文末衔接）。
> - **用户状态由 3 态改为 5 态**，并改为按「付费订单 + 体验课完课情况」动态计算（全平台口径，用户中心一期/二期、订单中心一致）。
> - 用户中心·二期新增**用户详情页**（课程信息 + 课时报告 + 回放）与**修改历史查看**。
> - 落地页新增**落地页名称（必填）**与**划线价（选填）**字段。
> - 商品包新增**创建时间**列，ID 规则化生成，移除独立详情弹窗。
>
> 说明：本文档为**规划 / 原型阶段**，原型内二期页面顶部均带「该模块为二期规划功能，当前页面仅作原型展示，相关功能将在二期上线」的提示。

---

# 背景

- 一期解决了「用户、订单查得到、看得清」，二期补齐**渠道精准归因、营销自助配置、权限治理**三块能力，并把「用户视图」从一张列表扩展为「列表 + 详情（含学习过程数据）」。

| 业务场景 | 一期怎么做 | 遗留问题 | 二期是否解决 |
| --- | --- | --- | --- |
| 渠道归因 | 应用商店来源 + 国家粗归因 | 无渠道树、无唯一 code，KOL 无法结算 | ✅ 渠道管理（渠道树 + 7 位 code + 渠道参数） |
| 商品与促销配置 | 找研发改配置 | 上新 / 调价 / 发券慢，依赖研发 | ✅ 商品包 / 优惠券自助配置 |
| 投放链接生成 | 研发手工拼 URL | 易出错、不可复用、无归因 | ✅ 落地页一键生成（带渠道 / 包 / 券 / 参数 / 划线价） |
| 学习过程可视 | 无 | 运营 / 客服看不到用户上了哪些课、体验/正式课报告 | ✅ 用户详情页课程信息（课时 + 报告 + 回放） |
| 数据权限 | 全员可见全部 | 无法按业务线 / 岗位隔离，无操作审计 | ✅ 系统配置 · 角色权限（RBAC + 业务线隔离 + 日志） |
| 付费转化跟进 | 无系统抓手 | 已注册未付费用户无人跟进 | ⏳ 三期（销售中心 + 外呼 / 通话记录） |

- 在线原型：<https://jiangshuang12345-tech.github.io/CRM/>（纯前端 Mock，登录仅支持 `@dinoai.ai` 邮箱；右上角可切换「查看身份」以模拟不同角色权限）。
- 一期 PRD：见同仓库《一期-基础查询-PRD.md》。

---

# 目标

- 二期定位：**从「查得到」升级到「归得准、配得动、看得透、管得住」**。
- 二期范围（3 大能力域）：
  1. **精准归因 + 用户视图**：渠道管理 + 用户中心·二期（归因到「业务线·渠道·code」，并可下钻用户详情看学习过程）。
  2. **营销自助**：商品包、优惠券、落地页自助配置与投放链接生成。
  3. **权限治理**：角色权限、业务线数据隔离、成员账号、操作日志。
- 衡量指标（建议）：
  - 渠道归因覆盖率 ≥ 95%
  - 商品 / 促销 / 落地页配置自助率 ≥ 90%（无需研发介入）
  - 全平台操作可审计（关键写操作 100% 落日志）

---

# 名词约定

- **业务线（Business Line）**：按国家 / 地区划分的运营单元（韩国 / 沙特 / 马来 / 越南 / 泰国 / 印尼 / 其他…）。二期作为渠道树顶层、数据隔离维度与全局筛选维度，替代一期的「国家」口径。
- **渠道类型 / 渠道层级**：业务线下的渠道大类（自然流量 / landingpage / KOL…）及其树状层级（一/二/三级，可扩展）。
- **渠道 code**：在渠道节点上生成的 **7 位随机字符串**（大小写字母 + 数字，去除易混字符），**系统自动去重、全局唯一**，用于归因与投放链接。仅 **Web 投放**场景有值。
- **投放类型**：**Web 投放**（配置渠道 ID / 渠道 code，走平台渠道体系精准归因）/ **App 投放**（平台无法透传自有参数，依赖第三方归因平台数据）。
- **投放渠道**：用户归因来源的展示字段——Web 投放为平台内渠道路径（业务线 · 渠道）；App 投放·第三方回传为第三方平台回传的渠道数据；App 投放·无回传（自然流量）为空。
- **渠道参数（param1 / param2）**：在已生成 code 的渠道节点上填写的自定义参数（暂 2 个），生成落地页链接时带入查询串（`&p1=…&p2=…`）。
- **商品包（Course Package）**：可售卖课程包，含业务线、名称、币种、价格、有效期、创建人、创建时间、状态（上架 / 下架）。
- **优惠券 / 优惠码**：一期仅支持**满减券**；一张券可含**多个优惠码**（按 KOL 生成），每码独立统计使用量用于结算。
- **落地页（Landing Page）**：关联渠道 code、商品包、优惠码、渠道参数，按业务线模版一键生成的投放链接；含**落地页名称**与可选**划线价**。
- **用户状态（5 态，系统动态计算）**：`未付费-未体验 / 未付费-体验中 / 未付费-已体验 / 付费 / 付费逾期`。按「是否有付费订单 + 体验课完课 / 进行中情况」自动推导，不可手工编辑（判定口径见「用户中心·二期」）。
- **课时记录（LessonRecord）**：用户上课记录，分**体验课 / 正式课**，含课标（courseLabel）、授课老师、完课时间、状态（进行中 / 已完课 / 已预约 / 已取消）、报告与回放链接。
- **课时报告**：体验课 → **Trial Report**（跳转外部报告页）；正式课 → **Lesson Report**（内部弹窗，含能力评估评分、课堂小结、老师评语、课后作业）。
- **角色 / 权限级别**：角色（Role）绑定各模块权限——**无权限（菜单不可见）/ 只读（仅查看、导出）/ 可操作（查看 + 新增 / 编辑）**。
- **数据范围（Data Scope）**：角色数据可见范围——**全部业务线** 或 **指定业务线**（账号级指定）。
- **【待确认】**：需与研发 / 投放 / 教研 / 财务侧拉齐口径的内容，见文末清单。

---

# 需求描述

二期在一期「查询主线」上扩展为「营销 - 用户视图 - 权限」体系，导航结构如下：

```Plain Text
┌──────────────── CRM 二期 ────────────────┐
│  [用户中心（一期）]      ← 一期保留         │
│  [订单中心]              ← 一期保留         │
│                                           │
│  [用户中心·二期]（二期）                    │
│     · 业务线 · 渠道 · 渠道 code 精准归因     │
│     · 业务线全局筛选 + 用户分流 + 5 态状态    │
│     · 用户ID 下钻 → 用户详情页               │
│            └─ 课程信息（课时 + 报告 + 回放）  │
│                                           │
│  [营销中心]（二期，一级菜单分组）            │
│     ├─ 渠道管理  → 渠道树 + code(自动去重)   │
│     ├─ 商品包    → 上新 / 调价 / 上下架      │
│     ├─ 优惠券    → 满减券 + 一券多码         │
│     └─ 落地页    → 一键生成投放链接(名称/划线价)│
│                                           │
│  [系统配置·角色权限]（二期）                 │
│     · 角色 / 权限矩阵 · 成员账号 · 操作日志  │
│                                           │
│  [销售中心]（三期，紫色标识）                │
│     · 线索池 / 我的跟进 / 外呼 / 通话记录     │← 见「与三期的衔接」
└───────────────────────────────────────────┘
```

数据流：**渠道管理生 code → 落地页带 code/包/券/划线价生成投放链接 → 用户注册携带 code → 用户中心·二期按 code 归因并可下钻用户详情看课程与报告 → 订单 / 权益回流**。全流程受角色权限与业务线数据范围管控。

---

## 〇、平台能力（二期增量）

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 全局筛选 | 业务线筛选 | 1. 用户中心·二期提供「业务线」筛选，且该筛选值**全局共享**（切换后其他关联页面沿用）<br>2. 数据范围为「指定业务线」的角色，筛选项仅列出其被授权的业务线 | P0 |
| 菜单权限 | 按权限渲染 | 1. 菜单按当前角色权限动态渲染：`无权限` 的模块菜单不展示<br>2. 直接访问无权限路由时重定向到第一个可访问页面 | P0 |
| 菜单分组 | 营销中心分组 | 渠道管理 / 商品包 / 优惠券 / 落地页 收拢到「营销中心」一级菜单下；用户中心·二期、系统配置独立陈列；销售中心（三期）单独置于菜单底部 | P1 |
| 用户状态 | 5 态动态计算 | 全平台统一按 5 态展示用户状态（一期用户中心、二期用户中心、订单中心一致），由系统按「付费订单 + 体验课完课情况」动态推导，不可手工编辑 | P0 |
| 身份切换（原型） | 查看身份 | 原型右上角可切换「查看身份」以模拟不同账号 / 角色的权限与数据范围 | P2 |

---

## 一、用户中心·二期（P0）

在一期用户中心基础上：将归因升级为「业务线 · 渠道 · 渠道 code」；用户状态升级为 5 态动态计算；**新增用户详情页**（下钻查看课程信息 / 课时报告 / 回放）；「最近修改人」可查看修改历史。**保留一期的用户类型、编辑、修改历史等能力。**

**用户故事**：作为运营 / 客服，我希望在用户列表看到业务线 / 渠道 / code 与用户状态，点用户ID 进入详情页查看其基本信息与已完课课时（体验课 / 正式课）、打开课时报告与课程回放，从而判断学习进展与转化 / 服务时机。

> **一期 vs 二期（归因差异）**：一期注册渠道仅 App Store / Google Play + 国家；二期为**业务线 · 渠道路径 · 渠道 code** 精准归因，新增「渠道 code」列与「业务线」全局筛选。二期归因能力按**投放类型（Web / App）**区分，详见下方「归因口径」。

### 1.0 投放类型与归因口径（重要）

二期归因分为两条链路：**Web 投放**走平台自建渠道体系（渠道管理配置渠道 ID / 渠道 code）精准归因；**App 投放**因平台无法透传自有参数，只能依赖**第三方归因平台**回传的数据归因。据此，用户的归因字段按下表取值：

| 投放类型 | 归因方式 | 渠道 code | 业务线 | 国家 | 投放渠道 |
| --- | --- | --- | --- | --- | --- |
| **Web 投放** | 配置渠道 ID（渠道管理生成的渠道 code）精准归因 | 有（渠道 code） | 有（渠道所属业务线） | 注册国家 | 渠道路径（业务线 · 渠道） |
| **App 投放 · 第三方回传** | 直投 App 到媒体，依赖第三方归因平台回传渠道数据 | 空 | 空 | 注册国家 | 第三方回传的渠道数据 |
| **App 投放 · 无回传（自然流量）** | 无渠道数据，计为自然流量 | 空 | 空 | 注册国家 | 空 |

> 说明：
> - **国家**在三种情况下均取**用户注册时的国家**（始终有值）。
> - **业务线 / 渠道 code** 仅在 **Web 投放**下有值；**App 投放**（无论是否有第三方回传）业务线与渠道 code 均为空。
> - **投放渠道**是承载归因来源的展示字段：Web 投放为平台内渠道路径；App 第三方回传为第三方平台渠道数据；自然流量为空。
> - 因此用户列表 / 详情的归因列需**兼容空值**（App 投放场景业务线、渠道 code 显示「—」）。

### 1.1 用户列表

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 关键字搜索 | 支持 用户ID / 姓名 / 登录账号 模糊搜索 | P0 |
| 查询 | 条件筛选 | 可按 **业务线**（全局共享）、**用户类型**（正式 / 测试）、**用户状态**（5 态）筛选 | P0 |
| 列表 | 归因列 | 展示 **业务线**、**投放渠道**、**渠道 code**、**国家**；按投放类型取值（见 1.0），App 投放场景业务线 / 渠道 code 显示「—」 | P0 |
| 列表 | 用户状态（5 态） | 展示动态计算的用户状态（未付费-未体验 / 未付费-体验中 / 未付费-已体验 / 付费 / 付费逾期），配色区分 | P0 |
| 列表 | 用户分流 | 仅展示「无手机号的未付费未体验用户」及「体验 / 已付费及后续状态用户」；「未付费-未体验 + 有手机号」用户分流至销售中心（三期），页面顶部提示分流规则 | P0 |
| 列表 | 用户ID 下钻 | 点击「用户ID」→ 进入**用户详情页**（`/users-v2/:studentId`） | P0 |
| 列表 | 修改历史入口 | 点击「最近修改人」→ 弹窗展示修改历史（时间 / 字段变更前后 / 修改人） | P1 |
| 编辑 | 修改基本信息 | 1. 可编辑：姓名 / 年龄段 / 用户类型（仅第三方登录可改；手机号 / kakao 自动判定不可改）<br>2. **业务线只读**；用户状态系统流转不可编辑<br>3. 保存后更新「最近修改人」并按字段记录修改历史 | P1 |

**用户状态定义（5 态，系统动态计算、不可手工编辑）**

| 用户状态 | 判定口径 | 配色 |
| --- | --- | --- |
| 未付费-未体验 | 无付费订单，且无「已完课」体验课、无「进行中」体验课 | 灰 |
| 未付费-体验中 | 无付费订单，存在「进行中」体验课且无「已完课」体验课 | 金 |
| 未付费-已体验 | 无付费订单，存在任意一节「已完课」体验课 | 蓝 |
| 付费 | 持有正式包且在有效期内 | 绿 |
| 付费逾期 | 曾持有正式包、已过有效期且未续订 | 红 |

> 相比一期的 3 态（注册 / 付费 / 付费逾期），二期重新引入「体验」维度并细分为 3 个未付费子态，判定依赖课时（体验课）完课数据。**【待确认】** 体验课「进行中 / 已完课」状态与完课时间由教研 / 上课系统提供。

### 1.2 用户详情页（新增）

点击用户ID 进入，含「基本信息」与「课程信息」两块。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 基本信息 | 概览 | 展示：用户ID / 姓名 / 用户状态（5 态）/ 用户类型 / 年龄段 / 注册方式 / 登录账号 / 业务线 / 投放渠道 / 渠道 code / 国家 / 注册时间 / 到期时间（归因字段按投放类型取值，见 1.0，空值显示「—」；时间按业务线 / 国家换算本地时区） | P0 |
| 课程信息 | 已完课课时列表 | 展示该用户所有「已完课」课时（体验课 + 正式课），按完课时间倒序。列：课标 / 课时类型 / 老师 / 完课时间 / 报告 / 回放 | P0 |
| 课程信息 | 报告查看 | 1. **体验课 → Trial Report**：点击跳转外部报告原型页（新标签）<br>2. **正式课 → Lesson Report**：点击弹窗展示报告详情（有报告才可点，否则「—」） | P0 |
| 课程信息 | 课程回放 | 有回放链接时点击「回放」→ 新标签整屏播放视频（无真实录播用占位视频） | P1 |
| 权限 | 数据范围校验 | 详情页受数据范围限制：越权 / 不存在时提示「未找到该用户，或你没有查看权限」 | P0 |

**课时记录字段（LessonRecord）**

| 字段 | 说明 |
| --- | --- |
| 课标（courseLabel） | 课程标识，如 `TCELA-L1-U2-LC1-11` |
| 课时类型 | 体验课 / 正式课 |
| 状态 | 进行中 / 已完课 / 已预约 / 已取消（详情页课程信息仅展示「已完课」） |
| 授课老师 | 老师姓名 |
| 完课时间 | 已完课才有，UTC 存储、按用户业务线 / 国家换算本地时区展示 |
| 报告 | 体验课=Trial Report（外链）/ 正式课=Lesson Report（弹窗） |
| 回放 | 回放链接（可播放） |

**课时报告内容（LessonReport，正式课弹窗）**

| 模块 | 说明 |
| --- | --- |
| 能力评估 | 若干评估项，每项 1–5 星评分（只读展示） |
| 课堂小结 | 本节课总结 |
| 老师评语 | 授课老师评价 |
| 课后作业 | 选填 |

> **【待确认】** 课时 / 报告 / 回放的数据来源与接口（体验课 Trial Report 外链地址、正式课报告结构、回放视频存储与鉴权）；课标编码规则；一期用户中心同时提供「试听报告」快捷入口（跳 Trial Report），是否长期保留。

---

## 二、营销中心 · 渠道管理（P0）

维护「业务线 → 渠道类型 → 一/二/三级渠道」树状结构，在任意渠道节点生成唯一渠道 code，并在末级渠道填写渠道参数，作为归因与投放的基础。**渠道 code 主要服务于 Web 投放**（配置渠道 ID 进行精准归因）；App 投放不使用平台渠道 code，改由第三方归因平台回传数据（见「用户中心·二期 1.0 归因口径」）。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 渠道树 | 业务线管理 | 新增 / 重命名 / 删除业务线（渠道树顶层）；默认收起，展开状态记忆到本地 | P0 |
| 渠道树 | 渠道类型管理 | 业务线下新增 / 重命名 / 删除渠道类型（自然流量 / landingpage / KOL…） | P0 |
| 渠道树 | 多级渠道管理 | 渠道类型下新增 / 重命名 / 删除一/二/三级渠道；一期支持至三级、可扩展 | P0 |
| 渠道 code | 生成 / 查看 code | 1. 在任意渠道节点点「生成 code」→ 生成 **7 位随机 code**（大小写字母 + 数字，去除易混字符），生成后固定<br>2. **生成时自动与渠道树中已有 code 去重，保证全局唯一**；极端碰撞追加计数兜底<br>3. 已有 code 可查看并一键复制 | P0 |
| 渠道参数 | 填写参数 | 1. 已生成 code 的渠道可填 **param1 / param2** 两个自定义参数（选填）<br>2. 参数在落地页生成时带入链接查询串（`&p1=…&p2=…`）<br>3. 未生成 code 的节点不可填参数 | P1 |

**数据结构**

| 层级 | 说明 | 可否生成 code | 可否填参数 |
| --- | --- | --- | --- |
| 业务线 | 韩国 / 沙特 / 泰国 / 越南 / 印尼 / 其他… | 否 | 否 |
| 渠道类型 | 自然流量 / landingpage / KOL… | 否 | 否 |
| 一级 / 二级 / 三级渠道 | 具体渠道 / 投放位 / KOL | 是 | 是（需先有 code） |

> 渠道 code 已实现**生成时自动去重、保证全局唯一**（收集渠道树全部已有 code 后排除，极端碰撞追加计数兜底）。**【待确认】** code 是否需带业务线前缀；渠道 / code 删除时对历史归因数据与已生成落地页的影响。

---

## 三、营销中心 · 商品包（P0）

商品包自助配置，供订单 / 落地页 / 优惠券关联使用。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 搜索 + 筛选 | 支持按 商品包ID / 名称 搜索；按业务线筛选（筛选项来源于实际数据） | P0 |
| 列表 | 列表 + 分页 | 展示：ID / 业务线 / 名称 / 价格（币种 + 金额）/ 有效期（起~止）/ 创建人 / 状态（上架 / 下架）/ **创建时间** | P0 |
| 新增 / 编辑 | 商品包配置 | 1. 字段：业务线、名称、币种、价格、有效期（起止时间）<br>2. 币种随业务线联动（本地币种 + USD 可选）<br>3. 新增默认状态「上架」，系统生成商品包 ID，记录创建人与创建时间 | P0 |
| 上下架 | 状态切换 | 二次确认后在「上架 / 下架」间切换 | P0 |

> 相对上一版：列表新增「创建时间」列，商品包 ID 改为系统规则化生成；移除独立「详情」弹窗（信息并入列表）。币种映射见附录。

---

## 四、营销中心 · 优惠券（P0）

一期仅支持**满减券**。核心能力是**一券多码**：一张券按 KOL / 使用方生成多个优惠码，各码独立统计使用量用于结算。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 搜索 + 筛选 | 按 券ID / 名称 / 优惠码 / KOL 搜索；按业务线、状态（已生效 / 已结束）筛选 | P0 |
| 列表 | 列表 + 分页 | 展示：券ID / 名称 / 优惠码数量 / 业务线 / 币种 / 发放总量 / 剩余量 / 创建人 / 状态 | P0 |
| 生成券 | 选择业务类型 | 点「生成优惠券」→ 先选业务线 → 进入配置表单 | P0 |
| 生成券 | 基础 / 发放 / 使用 / 权益规则 | 基础（业务线锁定 / 券类型满减锁定 / 币种 / 名称 / 创建人）；发放（总量、优惠码列表、领取有效期）；使用（使用有效期、可用商品）；权益（满 X 减 Y，减额≤门槛） | P0 |
| 一券多码 | 优惠码管理 | 1. 输入 KOL / 使用方名称回车 → 生成 **12 位优惠码**（去除易混字符）<br>2. **生成时自动与全部已有优惠码及当前编辑中未保存的码去重，保证全局唯一**；极端碰撞追加计数兜底<br>3. KOL 不可重复；支持删除<br>4. 可复制全部码、或「KOL + 码」成对复制<br>5. 已生效券可追加 / 管理优惠码，并按码展示已用量 | P0 |
| 运营操作 | 编辑商品 / 延长 / 停止 | 编辑可用商品（≥1）；修改领取截止时间（改到未来可恢复「已生效」）；停止发放（置「已结束」） | P1 |
| 详情 | 查看详情 | 基础 / 发放 / 使用规则 + 可用商品 + 优惠码列表（含各码已用量与合计） | P1 |

**优惠码（CouponCode）字段**

| 字段 | 说明 |
| --- | --- |
| KOL / 使用方 | 该码的分发对象（结算维度） |
| 优惠码 code | 12 位随机码，全局唯一，可复制 |
| 已使用张数 | 该码累计使用量，用于结算 |

> **【待确认】** 优惠码使用量数据来源与刷新方式；剩余量扣减口径；是否扩展折扣 / 满赠等券类型（二期先满减）。

---

## 五、营销中心 · 落地页（P0）

将渠道 code、商品包、优惠码与渠道参数按业务线模版组装为投放链接，一键生成、复制、打开。**本次新增落地页名称与划线价。**

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 列表 | 列表 + 分页 | 展示：**落地页名称** / 业务线 / 渠道（路径 + code + 渠道参数）/ 商品包 / **划线价** / 优惠券（券ID + 码）/ 有效期 / 链接 / 创建人 / 创建时间 | P0 |
| 生成 | 选择配置 | 1. **落地页名称（必填）**<br>2. 业务线（决定落地页模版）<br>3. 渠道：从该业务线**已生成 code** 的渠道中选（展示路径 + code + 渠道参数）<br>4. 商品包（必填，部分模版带包ID）<br>5. **划线价（选填）**：落地页展示用的原价，列表以删除线展示<br>6. 优惠券（选填）→ 选券后可再选具体优惠码（KOL 码）<br>7. 投放有效期（起止，本地时间） | P0 |
| 生成 | 链接拼装 | 按业务线模版拼装：带 `channel=code`、（可选）`coupon=码`、渠道参数 `&p1/&p2`；无模版的业务线不可生成并提示 | P0 |
| 生成 | 预览 | 提交前可「预览链接」查看最终 URL | P1 |
| 操作 | 复制 / 打开 / 删除 | 列表内一键复制链接、新窗口打开、删除（二次确认） | P1 |

> 划线价仅用于落地页展示（不参与实际计价）。落地页模版按业务线内置（韩国带 `backurl` 登录跳转，越南 / 印尼 / 马来为 landingpage 登录页）；无模版业务线暂不支持生成。**【待确认】** 各业务线模版 URL 规则、划线价与实付价的展示关系、新增业务线模版维护方式。

---

## 六、系统配置 · 角色权限（P0）

RBAC 权限体系：定义角色的模块权限与数据范围，管理成员账号，记录关键操作日志。**权限全平台生效**（含一期模块）。

### 6.1 角色权限（Tab）

| 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- |
| 角色列表 | 展示：角色名（内置 / 预留标识）/ 职责说明 / 数据范围 / 成员数 | P0 |
| 新增 / 编辑角色 | 字段：名称、说明、数据范围（全部 / 指定业务线）、各模块权限级别（无权限 / 只读 / 可操作） | P0 |
| 删除角色 | 内置角色不可删除；被账号占用的角色不可删除（提示占用数） | P0 |
| 权限矩阵 | 行=模块、列=角色，展示各角色对每个模块的权限级别（附图例） | P1 |

**权限级别**：无权限（菜单不可见）/ 只读（仅查看、导出）/ 可操作（查看 + 新增 / 编辑）。
**数据范围**：全部业务线 / 指定业务线（账号级指定）。

**内置角色（原型预置，可参考）**

| 角色 | 职责 | 数据范围 |
| --- | --- | --- |
| 管理员 / 系统配置 | 账号鉴权、模版、角色权限 | 全部业务线 |
| 市场投放 / 增长 | 渠道管理、落地页（生码 + 归因 + 投放） | 指定业务线 |
| 运营 / 商业化 | 商品包、优惠券、落地页 | 指定业务线 |
| 客服 / 用户支持 | 用户中心、订单中心 | 指定业务线 |
| 学习服务 / 班主任（预留） | 体验跟进、续费引导、流失挽回（后期随服务节点引入） | 指定业务线 |

### 6.2 成员账号（Tab）

| 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- |
| 列表 | 姓名 / 登录邮箱 / 角色 / 数据范围（业务线）/ 状态（启用 / 停用）/ 最近登录 | P0 |
| 新增 / 编辑 | 姓名、邮箱、角色、业务线范围（角色为「指定业务线」时必填，「全部」时禁用并置空）、启用状态 | P0 |
| 启用 / 停用 | 列表内开关切换账号启用状态 | P0 |

### 6.3 操作日志（Tab）

| 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- |
| 日志列表 | 记录关键写操作：时间 / 操作人 / 动作（新增 / 编辑角色、新增 / 编辑 / 启停账号等）/ 操作对象 | P1 |

> **【待确认】** 操作日志覆盖范围（是否覆盖营销 / 用户编辑等全部写操作）与保留期限；账号与公司 SSO / 目录服务是否打通；数据范围隔离在后端的强制校验口径。

---

# 埋点（二期增量）

| event_id | event | 含义 | 触发时机 | 属性 |
| --- | --- | --- | --- | --- |
| crm_userv2_view | `screen_view` | 用户中心·二期曝光 | 页面加载完成 | — |
| crm_userv2_search | `ui_click` | 用户查询（二期） | 应用筛选 | filter=keyword/line/userType/status |
| crm_user_detail | `screen_view` | 用户详情页曝光 | 进入 `/users-v2/:id` | — |
| crm_user_history | `ui_click` | 查看修改历史 | 点「最近修改人」 | — |
| crm_lesson_report | `ui_click` | 查看课时报告 | 点 Trial / Lesson Report | kind=trial/lesson |
| crm_lesson_replay | `ui_click` | 查看课程回放 | 点「回放」 | — |
| crm_channel_edit | `ui_click` | 渠道树增改删 | 新增 / 重命名 / 删除节点 | op=addLine/addType/addLevel/rename/delete |
| crm_channel_code | `ui_click` | 生成 / 查看渠道 code | 点「生成 code」 | action=gen/view |
| crm_channel_param | `ui_click` | 保存渠道参数 | 保存参数弹窗 | — |
| crm_pkg_edit | `ui_click` | 商品包增改 / 上下架 | 保存 / 切换状态 | op=add/edit/onShelf/offShelf |
| crm_coupon_gen | `ui_click` | 生成优惠券 | 提交生成 | — |
| crm_coupon_code | `ui_click` | 管理优惠码 | 追加 / 删除码、复制 | — |
| crm_coupon_op | `ui_click` | 券运营操作 | 编辑商品 / 延长 / 停止 | op=editProducts/extend/stop |
| crm_landing_gen | `ui_click` | 生成落地页链接 | 提交生成 | line/channelCode |
| crm_sys_role | `ui_click` | 角色权限增改删 | 保存 / 删除角色 | op=add/edit/delete |
| crm_sys_account | `ui_click` | 成员账号增改 / 启停 | 保存 / 切换状态 | op=add/edit/enable/disable |

> 销售中心相关埋点（外呼 / 通话记录 / 领取 / 跟进）随销售中心划归**三期**，见三期文档。

---

# 附录 / 公共枚举

**业务线 · 币种 · 国家码映射**

| 业务线 | 本地币种 | 国家码 | 可选币种 |
| --- | --- | --- | --- |
| 韩国 | 韩元 KRW | +82 | KRW / USD |
| 沙特 | 里亚尔 SAR | +966 | SAR / USD |
| 马来 | 林吉特 MYR | +60 | MYR / USD |
| 越南 | 越南盾 VND | +84 | VND / USD |
| 其他 | 美元 USD | +1 | USD |

**渠道 code / 优惠码 生成规则**

| 类型 | 长度 | 字符集 | 说明 |
| --- | --- | --- | --- |
| 渠道 code | 7 位 | 大小写字母 + 数字（去除 0/O、1/l/I 等易混字符） | 渠道节点唯一标识，生成后固定；**生成时自动对渠道树已有 code 去重、保证全局唯一，极端碰撞追加计数兜底** |
| 优惠码 code | 12 位 | 大写字母 + 数字（去除易混字符） | 一券多码，按 KOL 分发结算；**生成时自动对全部已有码及编辑中未保存的码去重、保证全局唯一，极端碰撞追加计数兜底** |

**状态枚举**

- 用户状态（5 态，系统动态计算）：未付费-未体验 / 未付费-体验中 / 未付费-已体验 / 付费 / 付费逾期
- 课时类型：体验课 / 正式课
- 课时状态：进行中 / 已完课 / 已预约 / 已取消
- 商品包状态：上架 / 下架
- 优惠券状态：已生效 / 已结束
- 优惠券类型：满减券（二期仅此一种）
- 权限级别：无权限 / 只读 / 可操作
- 数据范围：全部业务线 / 指定业务线
- 账号状态：启用 / 停用

**受权限管控的模块（8 个）**

- 渠道管理 / 落地页 / 商品包 / 优惠券 / 用户中心 / 销售中心 / 订单中心 / 系统配置

**落地页模版（按业务线内置，示例）**

| 业务线 | 模版规则（示意） |
| --- | --- |
| 韩国 | `https://kr.dinoai.ai/website/signin/?backurl=<内层支付页(含 id/channel/coupon/参数)>` |
| 越南 | `https://vn.dinoai.ai/website/landingpage/signin/?channel=<code>&coupon=<码>&p1/&p2` |
| 印尼 | `https://in.dinoai.ai/website/landingpage/signin/?channel=<code>…` |
| 马来 | `https://ma.dinoai.ai/website/landingpage/signin/?channel=<code>…` |
| 其他 | 暂无模版，不支持生成（提示配置模版） |

---

# 待确认事项（开发前拉齐）

1. **用户状态（5 态）**：体验课「进行中 / 已完课」与完课时间由哪一侧系统提供；付费 / 付费逾期的有效期与续订口径。
2. **课程 / 报告 / 回放**：课时数据接口与课标编码规则；体验课 Trial Report 外链地址；正式课 Lesson Report 结构；回放视频存储与鉴权；一期用户中心「试听报告」快捷入口是否长期保留。
3. **归因链路（Web / App）**：Web 投放注册链路是否透传并落库「渠道 code」，用户中心·二期与订单如何按 code 反查业务线 / 渠道路径；**App 投放对接哪家第三方归因平台、回传字段与口径**（投放渠道数据格式、有无回传的判定、归因时效），以及第三方渠道数据是否需要与平台业务线做映射。
4. **渠道 code / 优惠码**：已实现生成时自动去重、保证全局唯一（含极端碰撞计数兜底）；待确认 code 是否带业务线前缀、渠道 / code 删除对历史归因与已生成落地页的影响。
5. **落地页**：各业务线模版 URL 规则；划线价与实付价的展示关系；新增业务线模版维护方式。
6. **优惠券**：优惠码使用量数据来源与刷新方式；剩余量扣减口径；是否扩展折扣 / 满赠等券类型。
7. **权限体系**：数据范围隔离后端强制校验；操作日志覆盖范围与保留期限；账号是否与公司 SSO / 目录服务打通。
8. **一期/二期并行**：一期版用户中心与二期版是否长期并行还是二期上线后替换；订单中心是否同步升级为按业务线归因。

---

# 与三期的衔接（仅说明，不在二期范围）

**销售中心（原二期 → 现三期）** 及其新增能力：

| 三期模块 | 说明 |
| --- | --- |
| 销售中心 · 线索池 / 我的跟进 | 「未付费-未体验 + 有手机号」用户自动进入线索池，销售「领取 → 跟进 → 转「已付费」回流用户中心」；跟进进度：待领取 / 跟进中 / 暂不跟进 / 已付费 |
| 销售中心 · 外呼拨号 | 坐席在跟进列表点「拨号」→ 模拟发起外呼（通话计时）→ 挂断填写通话小结（通话结果：已接通 / 无人接听、时长、跟进记录） |
| 销售中心 · 通话记录 | 外呼系统与 CRM 轻量集成：通话小结自动归档到「客户档案 + 销售跟进记录」，并在「通话记录」Tab 汇总（时间 / 客户 / 电话 / 业务线 / 通话结果 / 时长 / 跟进记录 / 坐席），受数据范围限制（非超管仅看本人坐席记录） |

> 二期沉淀的「业务线 · 渠道 · code、用户状态（含体验维度）、课程 / 报告数据」是三期销售中心线索圈选、外呼跟进与转化归因的承接依据。

---

# 关联资源

- 一期 PRD：《一期-基础查询-PRD.md》（同仓库）
- 整体规划 PRD：<https://jiangshuang12345-tech.github.io/CRM-PRD/#mapping>
- 在线原型：<https://jiangshuang12345-tech.github.io/CRM/>（橙色「二期」标识模块即本文档范围；紫色「三期」为销售中心）
- 试听报告原型页：<https://jiangshuang12345-tech.github.io/CT-Trial-Report/>
- 原型仓库：<https://github.com/jiangshuang12345-tech/CRM>
