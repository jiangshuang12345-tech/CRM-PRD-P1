# CRM 运营平台 · 一期「基础查询」PRD

> **本次更新（一期相关）**
> 1. **用户中心编辑项调整**：资料编辑由「性别 / 出生日期」改为 **年龄段**（3-5 / 6-8 / 9-12 / 13-17 / 18+）；新增「年龄段」字段。
> 2. **时间按时区展示**：所有时间字段（注册时间 / 到期时间 / 支付时间等）底层统一 **UTC 存储**，展示时按**用户注册国家换算为当地时间并标注时区**（如 `UTC+09:00`），见附录「国家 · 时区映射」。
> 3. **订单中心归因口径统一为「国家」**：筛选与列表由「业务线」改为 **国家**（按归属用户注册国家），与一期用户中心口径一致。
>
> **前序累计变更（重新分期）**
> - **渠道体系整体后移至二期**：渠道管理及「业务线 · 渠道 · 渠道 code」精准归因不在一期范围。一期用户归因降级为 **应用商店来源（App Store / Google Play）+ 国家（注册 IP）** 粗归因。
> - **用户状态细化为 5 态**：注册 / 体验中 / 体验逾期 / 付费 / 付费逾期（原「流失」更名为「付费逾期」，新增「体验逾期」）。
> - **多语言默认英文**：开放 英 / 韩 / 阿 / 中 四种，默认 English。
>
> 说明：渠道管理、用户中心·二期、商品包、优惠券、落地页、系统配置/角色权限均属**二期**，不纳入本文档（仅在「与二期衔接」处简述）。

# 背景

- Dino English 面向海外多业务线（韩国 / 沙特 / 马来 / 越南 等）开展投放与运营。当前业务依赖人工散表查询用户与订单、归因口径不统一、促销配置依赖研发。
- 一期先解决「用户、订单查得到、看得清」，让客服 / 运营 / 市场自助完成日常查询；此时**尚无渠道分级体系**，用户归因仅做粗粒度（应用商店来源 + 国家）：

| 业务场景 | 现在怎么做 | 当前问题 | 一期是否解决 |
| --- | --- | --- | --- |
| 查用户 | 找研发拉数 / 翻多张散表 | 非手机号注册（谷歌/FB/AppID）的用户难以查到；查询慢、口径乱 | ✅ 解决（自助查询）|
| 查订单 | 找研发或财务导出对账 | 客服无法自助核对实付金额、支付方式、支付时间、有效期 | ✅ 解决（自助查询）|
| 渠道归因 | 渠道用线下表格、code 人工拼 | 归因口径不统一、无唯一 code | ⏳ 二期（一期先按应用商店 + 国家粗归因）|

- 整体规划 PRD（含交互原型）地址：<https://jiangshuang12345-tech.github.io/CRM-PRD/#mapping>
- 在线原型：<https://jiangshuang12345-tech.github.io/CRM/>（纯前端 Mock，登录仅支持 `@dinoai.ai` 邮箱，验证码直接显示在页面提示中）

# 目标

- 一期定位：**先把「用户、订单查得到、看得清」做扎实**，让市场与客服同学自助完成日常查询，替代线下散表。
- 一期范围（两个查询模块 + 一项平台能力）：
  1. **用户中心（一期）**：多条件检索用户，支持 5 种注册方式与登录账号展示；归因仅按 **应用商店来源（App Store / Google Play）+ 国家**；可维护基础资料。
  2. **订单信息查询（订单中心）**：多条件检索订单，查看商品、归属用户、金额、支付信息与有效期（一期只查不改）。
  3. **平台能力**：多语言（英 / 韩 / 阿 / 中，默认英文）切换；登录限 `@dinoai.ai` 邮箱。
- 衡量指标：
  - 用户 / 订单查询自助率 ≥ 90%
  - （渠道归因覆盖率 ≥ 95% 为**二期**指标，一期不承诺）

# 名词约定 / 颜色图例

- **注册渠道（一期口径）**：一期仅区分应用商店来源 **App Store / Google Play**（无渠道树、无业务线、无渠道 code）。
- **国家**：用户注册时 IP 对应的国家。一期用户中心与订单中心**统一以「国家」**做展示与筛选（替代二期的「业务线」）。
- **注册方式**：用户注册时使用的账号类型，共 5 种（谷歌邮箱 / Facebook / kakao / 手机号 / AppID）。
- **登录账号**：用户登录使用的账号值（邮箱 / Facebook / kakao ID / AppID / 手机号；手机号场景已并入登录账号）。
- **年龄段**：用户资料中的年龄区间（3-5 / 6-8 / 9-12 / 13-17 / 18+），一期可编辑（替代原「性别 / 出生日期」编辑项）。
- **用户状态**：系统按商品包有效期与续订情况自动流转的状态（注册 / 体验中 / 体验逾期 / 付费 / 付费逾期），不可手工编辑。
- **时区展示**：时间字段底层统一 UTC 存储，按用户注册国家换算为当地时间展示并标注时区（见附录「国家 · 时区映射」）。
- **业务线**：按国家 / 地区划分的运营单元；为**二期**（渠道归因）能力，一期用户中心与订单中心均改用「国家」。
- **【待确认】**：需与研发 / 投放侧拉齐口径的内容，见各模块标注与附录清单。

# 需求描述

一期围绕「查询」一条主线，仅含用户中心（一期）与订单中心两个模块，并在平台层提供多语言与登录鉴权：

```Plain Text
[登录]  仅 @dinoai.ai 邮箱 + 验证码
   ↓
[平台能力]  多语言（英/韩/阿/中，默认英文）
   ↓
┌──────────────── CRM 一期：基础查询 ────────────────┐
│                                                    │
│  [用户中心（一期）]                                  │
│     · 多条件查询用户 + 修改基础资料                    │
│     · 归因：应用商店来源（App Store/Google Play）+ 国家 │
│             │ 用户ID 关联                            │
│             ▼                                       │
│  [订单中心]                                          │
│     · 按订单/用户/商品查询订单，对账核查 + 有效期        │
│                                                    │
└────────────────────────────────────────────────────┘

（二期再建设：渠道管理 → 用户中心·二期「业务线·渠道」精准归因
  → 商品包 / 优惠券 / 落地页 / 角色权限）
```

- 一期不做渠道分级与 code 归因；待二期渠道管理上线后，用户中心升级为「用户中心·二期」，按「业务线 · 渠道 · 渠道 code」精准归因。

---

## 〇、平台能力 & 登录鉴权（P0）

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 登录 | 登录页 | 1. 仅支持 `@dinoai.ai` 邮箱登录<br>2. 输入邮箱 → 点「获取验证码」→ 验证码下发<br>3. 输入验证码 → 登录进入平台 | P0 |
| 登录 | 邮箱校验 | 邮箱后缀非 `@dinoai.ai` → 提示「仅支持 @dinoai.ai 邮箱」，不下发验证码 | P0 |
| 登录 | 验证码校验 | 1. 验证码错误 / 过期 → 提示重新获取<br>2. 验证码有效期、错误次数限制以研发实现为准（建议 5 分钟 / 5 次） | P0 |
| 多语言 | 语言切换 | 1. 右上角支持切换 **English / 한국어 / العربية / 简体中文**，**默认 English**<br>2. 切换后平台文案随之切换；阿语注意 RTL 排版<br>3. 底层可接入更多语言，当前对外开放 4 种 | P0 |
| 时间展示 | 按时区换算 | 1. 所有时间字段底层统一 **UTC 存储**<br>2. 展示时按**用户注册国家**换算为当地时间，并标注时区（如 `UTC+09:00`）<br>3. 国家 → 时区映射见附录；未匹配国家默认按 UTC 展示 | P0 |

> 说明：业务线「全局共享筛选」为**二期**能力（用户中心·二期按渠道归因）；一期用户中心与订单中心均按「国家」筛选 / 展示。

---

## 一、用户中心（一期）（P0）

客服 / 运营按多条件检索用户，查看基本信息、注册方式与登录账号、状态与有效期，并可维护基础资料。**一期暂无渠道体系**：注册来源仅区分应用商店（App Store / Google Play），归属国家取注册时 IP 对应国家。

**用户故事**：作为客服同学，我希望输入用户ID / 姓名 / 登录账号即可查到用户，并看到其注册方式、登录账号、注册渠道（App Store / Google Play）、国家、注册时间、到期时间与当前状态——即使用户用谷歌邮箱 / Facebook / AppID 等**非手机号方式**注册也能查到。

> **一期 vs 二期（重要）**
> 一期用户中心**不展示**「业务线 / 渠道 / 渠道 code」，仅用 **注册渠道（App Store / Google Play）+ 国家** 做粗粒度归因；待二期渠道管理上线后，升级为「用户中心·二期」，按「业务线 · 渠道 · 渠道 code」精准归因。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 关键字搜索 | 支持 用户ID / 姓名 / 登录账号 **模糊搜索**（登录账号即覆盖手机号场景） | P0 |
| 查询 | 条件筛选 | 可按 **注册渠道（App Store / Google Play）**、**国家**、用户状态（注册/体验中/体验逾期/付费/付费逾期）筛选 | P0 |
| 列表 | 列表 + 分页 | 1. 展示基本信息字段（见数据字典）<br>2. 支持分页与每页条数<br>3. 列表实时按条件过滤 | P0 |
| 列表 | 注册方式 / 登录账号展示 | 1. 展示「注册方式」「登录账号」列；支持 5 种注册方式<br>2. 姓名列展示 App 内姓名（`Student_Name`）<br>3. 注册渠道列仅展示 App Store / Google Play；另有「国家」列 | P0 |
| 列表 | 到期时间 / 最近修改人 | 1. 展示会员 / 商品包到期时间，无则显示「—」<br>2. 展示最近一次资料修改的操作人，无则显示「—」 | P0 |
| 编辑 | 修改基本信息 | 1. 可编辑：姓名（`Student_Name`）/ **年龄段**（3-5 / 6-8 / 9-12 / 13-17 / 18+）<br>2. **国家只读、不可修改**<br>3. 点「修改信息」→ 弹窗编辑 → 保存，系统记录「最近修改人」<br>4. 用户状态为系统流转，不可编辑 | P1 |

**数据字典（列表字段）**

| 字段 | 说明 | 可编辑 |
| --- | --- | --- |
| 用户ID | 用户唯一标识（长数字ID） | 否 |
| 学生姓名 | 单一姓名字段，取 App 内 `Student_Name` | 可编辑 |
| 年龄段（新增） | 3-5 / 6-8 / 9-12 / 13-17 / 18+（替代原「性别 / 出生日期」编辑项） | 可编辑 |
| 注册方式 | 谷歌邮箱 / Facebook / kakao / 手机号 / AppID | 否 |
| 登录账号 | 邮箱 / Facebook / kakao ID / AppID / 手机号 | 否 |
| 注册渠道（一期口径） | 仅区分应用商店来源：App Store / Google Play（一期无渠道树） | 否 |
| 国家（一期口径） | 注册时 IP 对应的国家（替代二期的「业务线」展示） | 否 |
| 注册时间 | 底层 UTC 存储，按注册国家换算为当地时间展示并标注时区（如 `UTC+09:00`） | 否 |
| 到期时间 | 会员 / 商品包到期时间，按注册国家换算为当地时间展示并标注时区；无则显示「—」 | 否 |
| 用户状态 | 注册 / 体验中 / 体验逾期 / 付费 / 付费逾期 | 否（系统流转） |
| 最近修改人 | 最近一次资料修改的操作人，无则显示「—」 | 否（系统记录） |

**注册方式与登录账号映射**

| 注册方式 | 登录账号示例 | 是否带手机号 |
| --- | --- | --- |
| 谷歌邮箱 | 邮箱地址 | 否 |
| Facebook | 邮箱（可拿到时） | 否 |
| AppID | 邮箱（可拿到时） | 否 |
| kakao | 手机号 | 是 |
| 手机号 | 手机号 | 是 |

**用户状态定义（系统自动流转，不可手工编辑）**

| 用户状态 | 定义 | 判定口径 |
| --- | --- | --- |
| 注册 | 已注册未付费 | 完成注册，从未付费 |
| 体验中 | 已付费 7 天体验商品包，且在有效期内 | 持有体验包 且 当前时间 ≤ 体验有效期 |
| 体验逾期 | 体验包已到期，且未转为正式付费 | 曾持有体验包 且 当前时间 > 体验有效期 且 未付费正式包 |
| 付费 | 付费正式商品包，且在有效期内（含到期自动续订） | 持有正式包 且 当前时间 ≤ 有效期 |
| 付费逾期 | 付费正式商品包，已过有效期且未续订（原「流失」） | 曾持有正式包 且 当前时间 > 有效期 且 未续订 |

> 状态随有效期推移与续订结果变化：体验到期未转正式即为「体验逾期」；正式包到期自动续订成功仍为「付费」，到期未续订转为「付费逾期」。**【待确认】** 状态计算所需的「有效期 / 续订状态」字段由订单 / 履约侧提供。

**关键交互流程**

1. 输入关键字（用户ID/姓名/登录账号）或选择 注册渠道（App Store/Google Play）/ 国家 / 用户状态 → 列表实时过滤。
2. 查看「注册方式」「登录账号」列定位用户来源；非手机号方式登录账号为邮箱 / 第三方账号。
3. 点「修改信息」→ 弹窗编辑 姓名（`Student_Name`）/ 年龄段（国家只读）→ 保存，系统记录「最近修改人」。

---

## 二、订单信息查询（订单中心）（P0）

按多条件检索订单，查看商品、归属用户、金额与支付信息，支撑客服对账与退款核查。**一期仅做查询，不含创建 / 退款等写操作。**

**用户故事**：作为客服 / 财务同学，我希望按订单ID / 用户ID / 商品检索订单，并能按国家、订单状态、支付方式筛选，快速核对实付金额、支付时间与有效期。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 关键字搜索 | 支持 订单ID / 用户ID / 商品名称 **模糊搜索** | P0 |
| 查询 | 条件筛选 | 可按 **国家**（按归属用户注册国家）、订单状态（待支付/已支付/已退款/已取消）、支付方式 筛选 | P0 |
| 列表 | 列表 + 分页 | 1. 展示 原价 / 实付 / 币种 / 支付时间 / **有效期到期时间** 等字段<br>2. 时间字段按归属用户注册国家换算当地时间展示并标注时区<br>3. 支持分页 | P0 |
| 范围约束 | 只读 | 一期不含创建 / 退款 / 取消等写操作；退款等待权限体系（二期）就绪后再开放 | P0 |

> 一期订单中心按「国家」筛选（与用户中心一期口径一致）；二期升级为按「业务线」筛选并与用户中心·二期联动。

**数据字典（列表字段）**

| 字段 | 说明 |
| --- | --- |
| 订单ID | 订单唯一标识，如 `DN2026061800001` |
| 商品名称 / 用户ID | 所购商品 + 归属用户 |
| 国家 | 按归属用户的注册国家展示（替代原「所属业务线」列，与一期用户中心口径一致） |
| 用户状态 / 订单状态 | 用户状态 + 订单状态双标签 |
| 原价 / 实际付款金额 | 含币种，按本地币种或 USD 展示 |
| 支付方式 | App Store / Google Play / Stripe / PayPal |
| 成功支付时间 | 底层 UTC 存储，按归属用户注册国家换算当地时间展示并标注时区；未支付显示 `—` |
| 有效期到期时间 | 订单对应权益的到期时间，按归属用户注册国家换算当地时间展示并标注时区；无则显示「—」 |

---

# 与二期的衔接（仅说明，不在一期范围）

为便于研发预留扩展，列出一期数据将如何被二期复用（**以下均为二期实现**）：

| 二期模块 | 与一期的关系 |
| --- | --- |
| 渠道管理 | 新建「业务线 → 渠道类型 → 一/二/三级」渠道树，生成 7 位随机渠道 code |
| 用户中心·二期 | 把一期「应用商店来源 + 国家」升级为「业务线 · 渠道 · 渠道 code」精准归因，新增业务线全局筛选 |
| 商品包 / 优惠券 / 落地页 | 商品与促销自助配置、一券多码（KOL 结算）、投放链接一键生成 |
| 系统配置 · 角色权限 | RBAC（无/只读/可操作）、业务线数据隔离、操作日志；权限全平台生效 |

> 一期沉淀的「用户状态、注册渠道、国家、订单有效期」是二期渠道归因与后期触达/服务节点的圈选与承接依据。

---

# 埋点

| event_id | event | 含义 | 触发时机 | 属性 | 数据值 |
| --- | --- | --- | --- | --- | --- |
| crm_login | `ui_click` | 点击获取验证码 / 登录 | 登录页操作 | step | step=otp/login |
| crm_lang_switch | `ui_click` | 切换语言 | 点右上角语言切换 | lang | lang=en/ko/ar/zh（默认 en） |
| crm_user_view | `screen_view` | 用户中心曝光 | 用户页加载完成 | — | — |
| crm_user_search | `ui_click` | 用户查询 | 点搜索 / 应用筛选 | filter | filter=keyword/registerChannel/country/status |
| crm_user_edit | `ui_click` | 修改用户信息 | 保存修改弹窗 | — | — |
| crm_order_view | `screen_view` | 订单中心曝光 | 订单页加载完成 | — | — |
| crm_order_search | `ui_click` | 订单查询 | 点搜索 / 应用筛选 | filter | filter=keyword/country/orderStatus/payMethod |

---

# 附录 / 公共枚举

**注册渠道（一期口径）**

- App Store
- Google Play

> 一期仅区分应用商店来源；渠道分级与渠道 code 为二期能力。

**国家 / 业务线 · 币种 · 国家码映射**（订单中心展示币种用；二期渠道 / 商品包共用）

| 国家 / 业务线 | 本地币种 | 国家码 | 可选币种 |
| --- | --- | --- | --- |
| 韩国 | 韩元 KRW | +82 | KRW / USD |
| 沙特 | 里亚尔 SAR | +966 | SAR / USD |
| 马来 | 林吉特 MYR | +60 | MYR / USD |
| 越南 | 越南盾 VND | +84 | VND / USD |
| 其他 | 美元 USD | +1 | USD |

**国家 · 时区映射**

> 时间字段以 UTC 存储，按用户注册国家换算为下列时区的当地时间展示并标注时区；未匹配到的国家默认按 UTC 展示。

| 国家 / 业务线 | 时区（IANA） | UTC 偏移 |
| --- | --- | --- |
| 中国 | Asia/Shanghai | UTC+08:00 |
| 韩国 | Asia/Seoul | UTC+09:00 |
| 沙特 | Asia/Riyadh | UTC+03:00 |
| 越南 | Asia/Ho_Chi_Minh | UTC+07:00 |
| 印尼 | Asia/Jakarta | UTC+07:00 |
| 泰国 | Asia/Bangkok | UTC+07:00 |
| 马来 / 马来西亚 | Asia/Kuala_Lumpur | UTC+08:00 |
| 其他 / 未匹配 | UTC | UTC+00:00 |

**平台能力**

| 能力 | 说明 |
| --- | --- |
| 多语言 | English / 한국어 / العربية / 简体中文，右上角切换，**默认英文**（阿语注意 RTL 排版；底层已接入更多语言，当前对外开放 4 种） |
| 时间展示（按时区换算） | 所有时间字段（注册时间 / 到期时间 / 支付时间等）底层统一 UTC 存储，展示时按用户注册国家换算为当地时间并标注时区（如 `UTC+09:00`），映射见上方「国家 · 时区映射」 |
| 一期归因口径 | 用户中心（一期）与订单中心均按 应用商店来源（App Store / Google Play）+ 国家（注册 IP）做粗归因，无渠道树与业务线 |
| 登录鉴权 | 邮箱验证码登录，仅允许 `@dinoai.ai` |

**状态枚举**

- 订单状态：待支付 / 已支付 / 已退款 / 已取消
- 用户状态：注册 / 体验中 / 体验逾期 / 付费 / 付费逾期

**待确认事项（开发前拉齐）**

1. 应用商店来源（App Store / Google Play）与「国家」（注册 IP）字段的取数口径与可用性。
2. 用户 / 订单数据来源：新建库 vs 对接现有业务库。
3. 用户状态（含新增「体验逾期」「付费逾期」）、用户「到期时间」、订单「有效期到期时间」所需的有效期 / 续订状态字段由谁提供、实时计算还是定时刷新。
4. 「最近修改人」记录范围与是否需要完整操作日志 / 审计（二期角色权限模块统一建设）。
5. 「年龄段」取数来源（App 资料是否已有）与区间口径（3-5 / 6-8 / 9-12 / 13-17 / 18+）确认。
6. 时间按注册国家时区换算展示：国家 → 时区映射的完整清单与未匹配国家的兜底（默认 UTC）确认。

# 关联资源

- 整体规划 PRD：<https://jiangshuang12345-tech.github.io/CRM-PRD/#mapping>
- 规划仓库：<https://github.com/jiangshuang12345-tech/CRM-PRD>
- 原型仓库：<https://github.com/jiangshuang12345-tech/CRM>
- 在线原型：<https://jiangshuang12345-tech.github.io/CRM/>
