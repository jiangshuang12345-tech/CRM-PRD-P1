# CRM 运营平台 · 二期「营销 / 销售 / 权限」PRD

> **本文档范围**
> 本文档覆盖在线原型中所有带「二期」标识（橙色 Tag）的功能模块，是一期《基础查询 PRD》的延续。一期已交付「用户中心（一期）+ 订单中心」两个查询模块与登录 / 多语言 / 时区展示等平台能力；二期在此基础上补齐**渠道精准归因、营销自助配置、销售线索转化与角色权限体系**。
>
> **二期功能清单（原型「二期」标识对应）**
> 1. **销售中心**（新增）：已注册未付费 + 有手机号的用户自动进入线索池，销售「领取 → 跟进 → 转化」。
> 2. **用户中心·二期**（升级）：把一期「应用商店来源 + 国家」升级为「业务线 · 渠道 · 渠道 code」精准归因，新增业务线全局筛选与用户分流。
> 3. **营销中心**（一级菜单分组，含 4 个子模块）：
>    - **渠道管理**：业务线 → 渠道类型 → 一/二/三级渠道树，任意层级生成 7 位渠道 code，末级可填渠道参数。
>    - **商品包**：商品包自助配置、上下架。
>    - **优惠券**：满减券配置，一券多码（按 KOL 分发、分别统计使用量结算）。
>    - **落地页**：关联渠道 / 商品包 / 优惠码，一键生成投放链接。
> 4. **系统配置 · 角色权限**（新增）：RBAC（无权限 / 只读 / 可操作）、业务线数据隔离、成员账号管理、操作日志。
>
> **与一期的关系**
> - 一期的「用户中心（一期）」「订单中心」继续保留，不带二期标识；二期的「用户中心·二期」是升级版并行陈列（原型 `/users` 为一期版、`/users-v2` 为二期版）。
> - 二期所有功能受角色权限管控：无权限用户看不到对应菜单。
> - 说明：本文档为**规划 / 原型阶段**，原型内二期页面顶部均带「该模块为二期规划功能，当前页面仅作原型展示，相关功能将在二期上线」的提示。

---

# 背景

- 一期解决了「用户、订单查得到、看得清」，但仍存在三块能力缺口：
  1. **归因粗**：一期用户归因仅到「应用商店来源（App Store / Google Play）+ 国家」，无法定位到具体投放渠道 / KOL，无法核算渠道 ROI 与 KOL 结算。
  2. **配置依赖研发**：商品包、促销活动、投放落地页链接均需研发手工拼接，运营无法自助。
  3. **转化靠人肉**：一期把用户状态精简为 3 态（注册 / 付费 / 付费逾期）后，「已注册未付费」用户缺少系统化的销售跟进抓手；无权限体系导致数据无法按业务线隔离。

| 业务场景 | 一期怎么做 | 遗留问题 | 二期是否解决 |
| --- | --- | --- | --- |
| 渠道归因 | 应用商店来源 + 国家粗归因 | 无渠道树、无唯一 code，KOL 无法结算 | ✅ 渠道管理（渠道树 + 7 位 code + 渠道参数） |
| 商品与促销配置 | 找研发改配置 | 上新 / 调价 / 发券慢，依赖研发 | ✅ 商品包 / 优惠券自助配置 |
| 投放链接生成 | 研发手工拼 URL | 易出错、不可复用、无归因 | ✅ 落地页一键生成（带渠道 / 包 / 券 / 参数） |
| 付费转化 | 无系统抓手 | 已注册未付费用户无人跟进 | ✅ 销售中心（线索池 → 领取 → 跟进 → 转化） |
| 数据权限 | 全员可见全部 | 无法按业务线 / 岗位隔离，无操作审计 | ✅ 系统配置 · 角色权限（RBAC + 业务线隔离 + 日志） |

- 在线原型：<https://jiangshuang12345-tech.github.io/CRM/>（纯前端 Mock，登录仅支持 `@dinoai.ai` 邮箱；右上角可切换「查看身份」以模拟不同角色权限）。
- 一期 PRD：见同仓库《一期-基础查询-PRD.md》。

---

# 目标

- 二期定位：**从「查得到」升级到「归得准、配得动、转得成、管得住」**，让市场投放 / 运营 / 销售 / 管理员各岗位在同一平台自助完成日常工作。
- 二期范围（4 大能力域）：
  1. **精准归因**：渠道管理 + 用户中心·二期，把用户 / 订单归因到「业务线 · 渠道 · 渠道 code」。
  2. **营销自助**：商品包、优惠券、落地页自助配置与投放链接生成。
  3. **销售转化**：销售中心线索池与跟进流程。
  4. **权限治理**：角色权限、业务线数据隔离、成员账号、操作日志。
- 衡量指标（建议）：
  - 渠道归因覆盖率 ≥ 95%
  - 商品 / 促销 / 落地页配置自助率 ≥ 90%（无需研发介入）
  - 销售中心线索领取率 / 跟进转化率可统计
  - 全平台操作可审计（关键写操作 100% 落日志）

---

# 名词约定

- **业务线（Business Line）**：按国家 / 地区划分的运营单元（韩国 / 沙特 / 马来 / 越南 / 泰国 / 印尼 / 其他…）。二期作为渠道树顶层、数据隔离维度与全局筛选维度，替代一期的「国家」口径。
- **渠道类型（Channel Type）**：业务线下的渠道大类，如自然流量 / landingpage / KOL 等。
- **渠道层级（一/二/三级渠道）**：渠道类型下的树状层级，一期支持至三级、可扩展。任意层级均可生成渠道 code。
- **渠道 code**：在渠道节点上生成的 **7 位随机字符串**（大小写字母 + 数字，去除易混字符），全局唯一，用于归因与投放链接。
- **渠道参数（param1 / param2）**：在渠道节点上填写的自定义参数（暂支持 2 个），生成落地页链接时带入查询串（`&p1=…&p2=…`）。
- **商品包（Course Package）**：可售卖的课程包，含业务线、名称、币种、价格、有效期（起止时间）、状态（上架 / 下架）。
- **优惠券（Coupon）/ 优惠码（Coupon Code）**：一期仅支持**满减券**（满 X 减 Y）；一张券可包含**多个优惠码**（按 KOL / 使用方生成），每个码独立统计使用量用于结算。
- **落地页（Landing Page）**：关联渠道 code、商品包、优惠码与渠道参数，按业务线模版一键生成的投放链接。
- **销售线索**：用户状态为「注册」（已注册未付费）且**能拿到手机号**的用户，自动进入销售中心线索池。
- **跟进进度**：线索在销售中心的状态（待领取 / 跟进中 / 暂不跟进 / 已付费）。转「已付费」后用户状态改写为「付费」，离开销售中心回流用户中心。
- **角色 / 权限级别**：角色（Role）绑定各模块的权限级别——**无权限（菜单不可见）/ 只读（仅查看、导出）/ 可操作（查看 + 新增 / 编辑）**。
- **数据范围（Data Scope）**：角色的数据可见范围——**全部业务线** 或 **指定业务线**（如韩国团队只看韩国数据）。
- **成员账号**：内部使用者账号，绑定邮箱、角色与业务线范围，可启用 / 停用。
- **【待确认】**：需与研发 / 投放 / 财务侧拉齐口径的内容，见文末清单。

---

# 需求描述

二期在一期「查询主线」之上扩展为「营销 - 销售 - 权限」闭环，导航结构如下：

```Plain Text
┌──────────────── CRM 二期 ────────────────┐
│  [用户中心（一期）]      ← 一期保留        │
│  [订单中心]              ← 一期保留        │
│                                           │
│  [销售中心]（二期）                        │
│     · 线索池（待领取）→ 领取 → 我的跟进     │
│     · 转「已付费」→ 回流用户中心            │
│                                           │
│  [用户中心·二期]（二期）                    │
│     · 业务线 · 渠道 · 渠道 code 精准归因    │
│     · 业务线全局筛选 + 用户分流             │
│                                           │
│  [营销中心]（二期，一级菜单分组）           │
│     ├─ 渠道管理  → 渠道树 + code + 参数     │
│     ├─ 商品包    → 上新 / 调价 / 上下架     │
│     ├─ 优惠券    → 满减券 + 一券多码        │
│     └─ 落地页    → 一键生成投放链接         │
│                                           │
│  [系统配置·角色权限]（二期）                │
│     · 角色 / 权限矩阵 · 成员账号 · 操作日志 │
└───────────────────────────────────────────┘
```

数据流：**渠道管理生 code → 落地页带 code/包/券生成投放链接 → 用户注册携带 code → 用户中心·二期按 code 归因 → 未付费有手机号用户进销售中心跟进转化 → 订单/权益回流**。全流程受角色权限与业务线数据范围管控。

---

## 〇、平台能力（二期增量）

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 全局筛选 | 业务线筛选 | 1. 用户中心·二期提供「业务线」筛选，且该筛选值**全局共享**（切换后其他关联页面沿用）<br>2. 数据范围为「指定业务线」的角色，筛选项仅列出其被授权的业务线 | P0 |
| 菜单权限 | 按权限渲染 | 1. 菜单按当前角色权限动态渲染：`无权限` 的模块菜单不展示<br>2. 直接访问无权限路由时重定向到第一个可访问页面 | P0 |
| 菜单分组 | 营销中心分组 | 渠道管理 / 商品包 / 优惠券 / 落地页 收拢到「营销中心」一级菜单下作为子菜单；销售中心、用户中心·二期、系统配置独立陈列 | P1 |
| 身份切换（原型） | 查看身份 | 原型右上角可切换「查看身份」以模拟不同账号 / 角色的权限与数据范围（正式环境按登录账号解析角色） | P2 |

---

## 一、销售中心（P0）

已注册未付费且能拿到手机号的用户自动进入线索池，由销售「领取 → 跟进 → 转化」，形成付费转化抓手。**销售中心不支持手机号手动录入**，线索完全来自系统分流。

**用户故事**：作为销售，我希望看到「已注册未付费 + 有手机号」的线索池，能领取线索到「我的跟进」，记录每次跟进进度与备注、约定下次跟进时间；当用户付费后，该线索自动转为「已付费」并从我的列表移除、回流用户中心。

**分流规则（与用户中心·二期互斥）**
- 用户状态 = `注册`（已注册未付费）且**有手机号**（kakao / 手机号注册）→ 进入**销售中心线索池**。
- 无手机号（谷歌 / Facebook / AppID）或已付费及后续状态 → 展示在**用户中心·二期**。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 线索池（待领取） | 列表 + 搜索 | 1. 展示所有「待领取」线索（无领取人）<br>2. 支持关键字（手机号 / 用户ID / 姓名 / 国家 / 渠道来源 / 备注）搜索、按国家筛选<br>3. 列表展示用户ID / 姓名 / 用户类型 / 年龄段 / 登录账号 / 业务线 / 渠道（业务线·渠道）/ 注册时间 | P0 |
| 线索池 | 领取 | 点「领取」→ 线索归属当前销售，进度置为「跟进中」，写入跟进记录，自动切到「我的跟进」页 | P0 |
| 我的跟进 | 列表 + 筛选 | 1. 展示当前销售已领取的线索（超级管理员 / 全业务线可见全部）<br>2. 支持关键字搜索、按跟进进度（跟进中 / 暂不跟进）筛选<br>3. 增列：跟进进度 / 最新备注 / 下次跟进时间 / 最后更新时间 / 领取人 | P0 |
| 我的跟进 | 更新跟进 | 1. 弹窗编辑：跟进进度（跟进中 / 已付费 / 暂不跟进）、下次跟进时间、备注（必填）<br>2. 保存后写入跟进记录（时间倒序 Timeline：进度 + 备注 + 时间 + 操作人）<br>3. 选「已付费」→ 用户状态改写为「付费」，线索离开销售中心、回流用户中心 | P0 |
| 统计 | 线索计数 | 页面展示当前销售中心线索总数（已注册未付费 + 有手机号） | P2 |

**跟进进度枚举与配色**

| 进度 | 含义 | 配色 |
| --- | --- | --- |
| 待领取 | 线索在池中、尚无领取人 | 橙色 |
| 跟进中 | 已领取、销售正在跟进 | 蓝色 |
| 暂不跟进 | 已领取、暂时搁置（如家长仍在了解） | 灰色 |
| 已付费 | 跟进转化成功；触发用户状态→付费、回流用户中心 | 绿色 |

**关键交互流程**
1. 系统自动把「注册 + 有手机号」用户放入线索池「待领取」。
2. 销售在线索池搜索 / 筛选后点「领取」→ 进度变「跟进中」，进入「我的跟进」。
3. 销售点「更新跟进」记录进度 + 备注 + 下次跟进时间，历史按时间倒序留痕。
4. 跟进选「已付费」→ 用户状态改写「付费」，线索移出销售中心、回流用户中心。

> **【待确认】** 线索去重与回流规则（同一用户多次注册 / 退费后是否重回线索池）；跟进记录是否纳入统一操作日志。

---

## 二、用户中心·二期（P0）

在一期用户中心基础上，将归因升级为「业务线 · 渠道 · 渠道 code」，并新增业务线全局筛选与用户分流。**保留一期已有的用户类型、用户状态、修改历史等能力。**

**用户故事**：作为运营 / 客服，我希望在用户列表看到用户的业务线、渠道（业务线·渠道路径）与渠道 code，按业务线 / 用户类型 / 用户状态精准筛选；「已注册未付费 + 有手机号」的用户已分流到销售中心，此处只看无手机号的未付费用户与已付费及后续状态用户。

> **一期 vs 二期（归因差异）**
> 一期：注册渠道仅 App Store / Google Play + 国家。二期：**业务线 · 渠道路径 · 渠道 code** 精准归因，新增「渠道 code」列与「业务线」全局筛选。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 关键字搜索 | 支持 用户ID / 姓名 / 登录账号 模糊搜索 | P0 |
| 查询 | 条件筛选 | 可按 **业务线**（全局共享筛选）、**用户类型**（正式 / 测试）、**用户状态**（注册 / 付费 / 付费逾期）筛选 | P0 |
| 列表 | 精准归因列 | 新增 / 升级列：**业务线**、**渠道**（展示「业务线 · 渠道」路径）、**渠道 code** | P0 |
| 列表 | 用户分流 | 仅展示「无手机号的未付费用户」及「已付费及后续状态用户」；「注册 + 有手机号」用户分流至销售中心（页面顶部提示分流规则） | P0 |
| 列表 | 基础字段 | 沿用一期：用户ID / 姓名 / 用户状态 / 用户类型 / 注册方式 / 登录账号 / 注册时间 / 到期时间 / 最近修改人；分页 | P0 |
| 编辑 | 修改基本信息 | 1. 可编辑：姓名 / 年龄段 / 用户类型（仅第三方登录可改；手机号 / kakao 由规则自动判定不可改）<br>2. **业务线只读**、不可修改；用户状态系统流转不可编辑<br>3. 保存后更新「最近修改人」并按字段记录修改历史 | P1 |

**数据字典（列表字段，相对一期的增量以「新增/升级」标注）**

| 字段 | 说明 | 相对一期 |
| --- | --- | --- |
| 用户ID / 姓名 / 用户状态 / 用户类型 | 同一期 | 沿用 |
| 注册方式 / 登录账号 | 同一期（谷歌 / FB / kakao / 手机号 / AppID） | 沿用 |
| 业务线 | 用户所属业务线（韩国 / 沙特 / …） | 升级（替代国家） |
| 渠道 | 「业务线 · 渠道」路径（如 `韩国 · 自然流量 / ASO`） | 新增 |
| 渠道 code | 归因到的 7 位渠道 code | 新增 |
| 注册时间 / 到期时间 | UTC 存储，按业务线 / 国家换算本地时区展示 | 沿用 |
| 最近修改人 | 最近资料修改操作人（可查修改历史） | 沿用 |

> **【待确认】** 用户「业务线 / 渠道 / 渠道 code」的取数来源（注册链路是否透传 code 并落库）；一期版用户中心与二期版是否长期并行还是二期上线后替换。

---

## 三、营销中心 · 渠道管理（P0）

维护「业务线 → 渠道类型 → 一/二/三级渠道」的树状结构，在任意渠道节点生成唯一渠道 code，并在末级渠道填写渠道参数，作为归因与投放的基础。

**用户故事**：作为市场投放同学，我希望按业务线搭建渠道树（渠道类型 → 多级渠道），为具体渠道 / KOL 生成唯一 code，并填写渠道参数，供落地页生成投放链接、供用户中心归因。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 渠道树 | 业务线管理 | 新增 / 重命名 / 删除业务线（渠道树顶层）；默认收起，展开状态记忆到本地 | P0 |
| 渠道树 | 渠道类型管理 | 业务线下新增 / 重命名 / 删除渠道类型（自然流量 / landingpage / KOL…） | P0 |
| 渠道树 | 多级渠道管理 | 渠道类型下新增 / 重命名 / 删除一/二/三级渠道；一期支持至三级、可扩展 | P0 |
| 渠道 code | 生成 / 查看 code | 1. 在任意渠道节点点「生成 code」→ 生成 **7 位随机 code**（大小写字母 + 数字，去除易混字符），全局唯一、生成后固定<br>2. 已有 code 可查看并一键复制 | P0 |
| 渠道参数 | 填写参数 | 1. 已生成 code 的渠道可填 **param1 / param2** 两个自定义参数（选填）<br>2. 参数在落地页生成时带入链接查询串（`&p1=…&p2=…`）<br>3. 未生成 code 的节点不可填参数 | P1 |

**数据结构**

| 层级 | 说明 | 可否生成 code | 可否填参数 |
| --- | --- | --- | --- |
| 业务线 | 韩国 / 沙特 / 泰国 / 越南 / 印尼 / 其他… | 否 | 否 |
| 渠道类型 | 自然流量 / landingpage / KOL… | 否 | 否 |
| 一级 / 二级 / 三级渠道 | 具体渠道 / 投放位 / KOL | 是 | 是（需先有 code） |

> **【待确认】** 渠道 code 生成规则（是否需去重校验碰撞、是否带业务线前缀）；渠道 / code 删除时对历史归因数据与已生成落地页的影响。

---

## 四、营销中心 · 商品包（P0）

商品包自助配置，供订单 / 落地页 / 优惠券关联使用。

**用户故事**：作为运营，我希望自助新增 / 编辑商品包（业务线、名称、币种、价格、有效期），并控制上下架，无需研发介入。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 搜索 + 筛选 | 支持按 商品包ID / 名称 搜索；按业务线筛选 | P0 |
| 列表 | 列表 + 分页 | 展示：ID / 业务线 / 名称 / 价格（币种 + 金额）/ 有效期（起~止）/ 创建人 / 状态（上架 / 下架） | P0 |
| 新增 / 编辑 | 商品包配置 | 1. 字段：业务线、名称、币种、价格、有效期（起止时间）<br>2. 币种随业务线联动（本地币种 + USD 可选）<br>3. 新增默认状态「上架」，记录创建人与创建时间 | P0 |
| 上下架 | 状态切换 | 二次确认后在「上架 / 下架」间切换 | P0 |
| 详情 | 查看详情 | 查看商品包全部字段（含创建时间） | P1 |

> 币种映射：韩国 KRW / 沙特 SAR / 马来 MYR / 越南 VND / 其他 USD，均可另选 USD（见附录）。

---

## 五、营销中心 · 优惠券（P0）

一期仅支持**满减券**。核心能力是**一券多码**：一张券按 KOL / 使用方生成多个优惠码，各码独立统计使用量用于结算。

**用户故事**：作为运营，我希望创建满减券（满 X 减 Y），为不同 KOL 生成各自的优惠码，投放后按码统计使用量做结算；并能编辑可用商品、追加优惠码、延长领取时间或提前结束发放。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 查询 | 搜索 + 筛选 | 按 券ID / 名称 / 优惠码 / KOL 搜索；按业务线、状态（已生效 / 已结束）筛选 | P0 |
| 列表 | 列表 + 分页 | 展示：券ID / 名称 / 优惠码数量 / 业务线 / 币种 / 发放总量 / 剩余量 / 创建人 / 状态 | P0 |
| 生成券 | 选择业务类型 | 点「生成优惠券」→ 先选业务线 → 进入配置表单 | P0 |
| 生成券 | 基础信息 | 业务线（锁定）、券类型（满减券，锁定）、币种、券名称、创建人 | P0 |
| 生成券 | 发放规则 | 发放总量、优惠码列表（按 KOL 生成，见下）、领取有效期（起止） | P0 |
| 生成券 | 使用规则 | 使用有效期（起止）、可用商品（输入商品包ID 校验后添加，可多个） | P0 |
| 生成券 | 权益规则 | 满减：满 `threshold` 减 `deduct`（减额不得大于门槛） | P0 |
| 一券多码 | 优惠码管理 | 1. 输入 KOL / 使用方名称回车 → 生成 **12 位优惠码**（去除易混字符）<br>2. KOL 不可重复；支持删除<br>3. 可复制全部码、或「KOL + 码」成对复制<br>4. 已生效券可追加 / 管理优惠码，并按码展示已用量 | P0 |
| 运营操作 | 编辑可用商品 | 编辑已生效券的可用商品（至少 1 个） | P1 |
| 运营操作 | 延长时间 | 修改领取截止时间；改到未来时间可恢复为「已生效」 | P1 |
| 运营操作 | 停止发放 | 二次确认后将券置为「已结束」 | P1 |
| 详情 | 查看详情 | 基础 / 发放 / 使用规则 + 可用商品 + 优惠码列表（含各码已用量与合计） | P1 |

**优惠码（CouponCode）字段**

| 字段 | 说明 |
| --- | --- |
| KOL / 使用方 | 该码的分发对象（结算维度） |
| 优惠码 code | 12 位随机码，全局唯一，可复制 |
| 已使用张数 | 该码累计使用量，用于结算 |

> **【待确认】** 优惠码使用量的数据来源与刷新方式（实时 / 定时）；「剩余量」与各码使用量的扣减口径；是否支持折扣券 / 满赠等其他券类型（二期先满减）。

---

## 六、营销中心 · 落地页（P0）

将渠道 code、商品包、优惠码与渠道参数按业务线模版组装为投放链接，一键生成、复制、打开。

**用户故事**：作为市场投放同学，我希望选择业务线 → 渠道（已生 code）→ 商品包 →（可选）优惠券 / 优惠码，系统按该业务线的落地页模版自动拼出带归因参数的投放链接，可预览、复制、直接打开。

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 列表 | 列表 + 分页 | 展示：业务线 / 渠道（路径 + code + 渠道参数）/ 商品包 / 优惠券（券ID + 码）/ 有效期 / 链接 / 创建人 / 创建时间 | P0 |
| 生成 | 选择配置 | 1. 业务线（决定使用哪个落地页模版）<br>2. 渠道：从该业务线**已生成 code** 的渠道中选（展示路径 + code + 渠道参数）<br>3. 商品包（必填，部分模版带包ID）<br>4. 优惠券（选填）→ 选券后可再选具体优惠码（KOL 码）<br>5. 投放有效期（起止，本地时间） | P0 |
| 生成 | 链接拼装 | 按业务线模版拼装：带 `channel=code`、（可选）`coupon=码`、渠道参数 `&p1/&p2`；无模版的业务线不可生成并提示 | P0 |
| 生成 | 预览 | 提交前可「预览链接」查看最终 URL | P1 |
| 操作 | 复制 / 打开 / 删除 | 列表内一键复制链接、新窗口打开、删除（二次确认） | P1 |

> 落地页模版按业务线内置（如韩国带 `backurl` 登录跳转、越南 / 印尼 / 马来为 landingpage 登录页）；无模版业务线暂不支持生成。**【待确认】** 各业务线模版 URL 规则与新增业务线模版的维护方式。

---

## 七、系统配置 · 角色权限（P0）

RBAC 权限体系：定义角色的模块权限与数据范围，管理成员账号，记录关键操作日志。**权限全平台生效**（含一期模块）。

**用户故事**：作为管理员，我希望配置角色（每个模块的无权限 / 只读 / 可操作）与数据范围（全部 / 指定业务线），为成员账号分配角色与业务线，并能启用 / 停用账号、查看操作日志。

### 7.1 角色权限（Tab）

| 一级模块 | 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- | --- |
| 角色列表 | 列表 | 展示：角色名（内置 / 预留标识）/ 职责说明 / 数据范围 / 成员数 | P0 |
| 角色 | 新增 / 编辑 | 1. 字段：名称、说明、数据范围（全部业务线 / 指定业务线）、各模块权限级别<br>2. 权限矩阵逐模块选 无权限 / 只读 / 可操作 | P0 |
| 角色 | 删除 | 内置角色不可删除；被账号占用的角色不可删除（提示占用数） | P0 |
| 权限矩阵 | 矩阵总览 | 行=模块、列=角色，展示各角色对每个模块的权限级别（附图例） | P1 |

**权限级别定义**

| 级别 | 含义 |
| --- | --- |
| 无权限（none） | 菜单不可见 |
| 只读（view） | 仅查看 / 导出 |
| 可操作（operate） | 查看 + 新增 / 编辑 |

**数据范围定义**

| 范围 | 含义 |
| --- | --- |
| 全部业务线（all） | 可查看所有业务线数据 |
| 指定业务线（line） | 仅可查看被分配的业务线数据（账号级指定） |

**内置角色（原型预置，可参考）**

| 角色 | 职责 | 数据范围 | 典型权限 |
| --- | --- | --- | --- |
| 管理员 / 系统配置 | 账号鉴权、模版、角色权限 | 全部业务线 | 全模块可操作 |
| 市场投放 / 增长 | 渠道管理、落地页（生码 + 归因 + 投放） | 指定业务线 | 渠道 / 落地页可操作；商品包 / 优惠券只读 |
| 运营 / 商业化 | 商品包、优惠券、落地页 | 指定业务线 | 营销模块可操作；用户 / 销售 / 订单只读 |
| 客服 / 用户支持 | 用户中心、订单中心 | 指定业务线 | 用户 / 销售可操作；订单只读 |
| 学习服务 / 班主任（预留） | 体验跟进、续费引导、流失挽回 | 指定业务线 | 销售可操作；用户 / 订单只读（后期随服务节点引入） |

### 7.2 成员账号（Tab）

| 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- |
| 列表 | 展示：姓名 / 登录邮箱 / 角色 / 数据范围（业务线）/ 状态（启用 / 停用）/ 最近登录 | P0 |
| 新增 / 编辑 | 字段：姓名、邮箱、角色、业务线范围（角色为「指定业务线」时必填，「全部」时禁用并置空）、启用状态 | P0 |
| 启用 / 停用 | 列表内开关切换账号启用状态 | P0 |

### 7.3 操作日志（Tab）

| 二级模块 | 描述（产品逻辑） | 优先级 |
| --- | --- | --- |
| 日志列表 | 记录关键写操作：时间 / 操作人 / 动作（如新增角色 / 编辑角色 / 新增账号 / 启用停用账号）/ 操作对象 | P1 |

> **【待确认】** 操作日志的覆盖范围（是否覆盖营销 / 销售 / 用户编辑等全部写操作）与保留期限；账号与公司 SSO / 目录服务是否打通；数据范围隔离在后端的强制校验口径。

---

# 埋点（二期增量）

| event_id | event | 含义 | 触发时机 | 属性 |
| --- | --- | --- | --- | --- |
| crm_sales_view | `screen_view` | 销售中心曝光 | 销售中心加载完成 | tab=pool/follow |
| crm_sales_claim | `ui_click` | 领取线索 | 点「领取」 | — |
| crm_sales_follow | `ui_click` | 更新跟进 | 保存跟进弹窗 | progress=跟进中/暂不跟进/已付费 |
| crm_userv2_view | `screen_view` | 用户中心·二期曝光 | 页面加载完成 | — |
| crm_userv2_search | `ui_click` | 用户查询（二期） | 应用筛选 | filter=keyword/line/userType/status |
| crm_channel_edit | `ui_click` | 渠道树增改删 | 新增 / 重命名 / 删除节点 | op=addLine/addType/addLevel/rename/delete |
| crm_channel_code | `ui_click` | 生成 / 查看渠道 code | 点「生成 code」 | action=gen/view |
| crm_channel_param | `ui_click` | 保存渠道参数 | 保存参数弹窗 | — |
| crm_pkg_edit | `ui_click` | 商品包增改 / 上下架 | 保存 / 切换状态 | op=add/edit/onShelf/offShelf |
| crm_coupon_gen | `ui_click` | 生成优惠券 | 提交生成 | — |
| crm_coupon_code | `ui_click` | 管理优惠码 | 追加 / 删除码、复制 | — |
| crm_coupon_op | `ui_click` | 券运营操作 | 编辑商品 / 延长 / 停止 | op=editProducts/extend/stop |
| crm_landing_gen | `ui_click` | 生成落地页链接 | 提交生成 | line/channelCode |
| crm_sys_role | `ui_click` | 角色权限增改删 | 保存 / 删除角色 | op=add/edit/delete |
| crm_sys_account | `ui_click` | 成员账号增改 / 启停 | 保存 / 切换状态 | op=add/edit/enable/disable |

---

# 附录 / 公共枚举

**业务线 · 币种 · 国家码映射**

| 业务线 | 本地币种 | 国家码 | 可选币种 |
| --- | --- | --- | --- |
| 韩国 | 韩元 KRW | +82 | KRW / USD |
| 沙特 | 里亚尔 SAR | +966 | SAR / USD |
| 马来 | 林吉特 MYR | +60 | MYR / USD |
| 越南 | 越南盾 VND | +84 | VND / USD |
| 其他 | 美元 USD | +1 | USD |

**渠道 code / 优惠码 生成规则**

| 类型 | 长度 | 字符集 | 说明 |
| --- | --- | --- | --- |
| 渠道 code | 7 位 | 大小写字母 + 数字（去除 0/O、1/l/I 等易混字符） | 渠道节点唯一标识，生成后固定 |
| 优惠码 code | 12 位 | 大写字母 + 数字（去除易混字符） | 一券多码，按 KOL 分发结算 |

**状态枚举**

- 跟进进度：待领取 / 跟进中 / 暂不跟进 / 已付费
- 商品包状态：上架 / 下架
- 优惠券状态：已生效 / 已结束
- 优惠券类型：满减券（二期仅此一种）
- 权限级别：无权限 / 只读 / 可操作
- 数据范围：全部业务线 / 指定业务线
- 账号状态：启用 / 停用

**受权限管控的模块（8 个）**

- 渠道管理 / 落地页 / 商品包 / 优惠券 / 用户中心 / 销售中心 / 订单中心 / 系统配置

**落地页模版（按业务线内置，示例）**

| 业务线 | 模版规则（示意） |
| --- | --- |
| 韩国 | `https://kr.dinoai.ai/website/signin/?backurl=<内层支付页(含 id/channel/coupon/参数)>` |
| 越南 | `https://vn.dinoai.ai/website/landingpage/signin/?channel=<code>&coupon=<码>&p1/&p2` |
| 印尼 | `https://in.dinoai.ai/website/landingpage/signin/?channel=<code>…` |
| 马来 | `https://ma.dinoai.ai/website/landingpage/signin/?channel=<code>…` |
| 其他 | 暂无模版，不支持生成（提示配置模版） |

---

# 待确认事项（开发前拉齐）

1. **归因链路**：注册链路是否透传并落库「渠道 code」，用户中心·二期与订单如何按 code 反查业务线 / 渠道路径。
2. **渠道 code**：生成是否需碰撞校验、是否带业务线前缀；渠道 / code 删除对历史归因与已生成落地页的影响。
3. **销售中心**：线索去重与回流规则（重复注册 / 退费）；跟进记录是否纳入统一操作日志；「我的跟进」可见范围与超管例外。
4. **优惠券**：优惠码使用量数据来源与刷新方式；剩余量扣减口径；是否扩展折扣 / 满赠等券类型。
5. **落地页**：各业务线模版 URL 规则与新增业务线模版维护方式；链接短链 / 追踪需求。
6. **权限体系**：数据范围隔离在后端的强制校验；操作日志覆盖范围与保留期限；账号是否与公司 SSO / 目录服务打通。
7. **一期/二期并行**：一期版用户中心与二期版是否长期并行还是二期上线后替换；订单中心是否同步升级为按业务线归因。

---

# 关联资源

- 一期 PRD：《一期-基础查询-PRD.md》（同仓库）
- 整体规划 PRD：<https://jiangshuang12345-tech.github.io/CRM-PRD/#mapping>
- 在线原型：<https://jiangshuang12345-tech.github.io/CRM/>（带「二期」标识的模块即本文档范围）
- 原型仓库：<https://github.com/jiangshuang12345-tech/CRM>
